<div>
  {{#each players as |item|}}
    <PlayerRow id={{item.id}} @player={{item}} 
      @onRemove={{action "destroyPlayer" item}} 
      @onUpdate={{action "updatePlayer" item}}
    />
  {{else}}
    No players found.
  {{/each}}
</div>


